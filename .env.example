# CFB Data API — https://collegefootballdata.com/key
# The only credential the pipeline needs. All data is stored as local JSON
# under data/raw/ and data/computed/ — there is no database.
CFB_API_KEY=your_cfb_data_api_key
